import { Model } from "mongoose";
import { ContructorDetails } from "../app";
import { ICounter } from "../db/models/counter";
import { ITransaction } from "../db/models/transaction";
export declare class MainController {
    static params: ContructorDetails;
    static TransactionModel: Model<ITransaction>;
    static CounterModel: Model<ICounter>;
    static handler(request: any, TransactionModel: Model<ITransaction>, CounterModel: Model<ICounter>, params: ContructorDetails): Promise<{
        allow: boolean;
    } | {
        create_time: number;
        transaction: string;
        state: number;
    } | {
        transaction: string;
        perform_time: number;
        state: number;
    } | {
        transaction: string;
        cancel_time: number;
        state: number;
    } | {
        transactions: any[];
    }>;
    private static switchMethod;
    private static validatePaymeAmount;
    private static CheckPerformTransaction;
    private static CreateTransaction;
    private static PerformTransaction;
    private static CancelTransaction;
    private static CheckTransaction;
    private static GetStatement;
}
